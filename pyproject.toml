[project]
name = "polysport"
version = "0.1.0"
description = "Polymarket sports betting trader (paper-first)."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
